import axios from "axios";

export async function handleUpdateSettings(userId) {
  const url =
    "https://course.cta.uat.api.codibot.ai/api/v1.5.0/resource?resource_type=user&user_id=${user_id}";
}
